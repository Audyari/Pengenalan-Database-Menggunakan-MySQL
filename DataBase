Membuat, Menambahkan Dan Menghapus Primary Key

=================================================

1. CREATE TABLE mahasiswa (
    id INT PRIMARY KEY,
    nama VARCHAR(50),
    usia INT
    );

2. ALTER TABLE mahasiswa DROP PRIMARY KEY;

3. DESCRIBE mahasiswa;

4. ALTER TABLE mahasiswa ADD PRIMARY KEY(nama);

====================================================
