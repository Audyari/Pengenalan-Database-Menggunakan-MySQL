Membuat, Menambahkan Dan Menghapus Unique Key

=============================================

1. CREATE TABLE mahasiswa (
      id INT,
      nama VARCHAR(50) UNIQUE,
      usia INT
    );

2. ALTER TABLE mahasiswa ADD UNIQUE (usia);

3. ALTER TABLE mahasiswa DROP INDEX nama;

================================================
