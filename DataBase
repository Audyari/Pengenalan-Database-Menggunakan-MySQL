Import File SQL di MySQL dari Data Generator

gunakan link web untuk menggeneret data : 

1. https://mockaroo.com/

liat pake SELECT * FROM customers;

2. gunakan "source"

3. contoh : MariaDB [nama_database]> source C:\Users\itadmin\Downloads\customers.sql

=========================

set foreign_key_checks = 0;
truncate table customers;
set foreign_key_checks = 0;

==========================

1. set foreign_key_checks = 0;
Perintah ini menonaktifkan pemeriksaan batasan (constraint) foreign key di database.
Ini dilakukan untuk memungkinkan tabel "customers" untuk dikosongkan dengan perintah TRUNCATE tanpa terkendala oleh batasan foreign key yang mungkin ada.

2. truncate table customers;
Perintah TRUNCATE TABLE digunakan untuk menghapus seluruh isi (data) dari tabel "customers" secara cepat.
TRUNCATE bekerja lebih cepat daripada DELETE karena tidak mencatat setiap penghapusan dalam log transaksi.

3. set foreign_key_checks = 0;
Perintah ini kembali mengaktifkan pemeriksaan batasan (constraint) foreign key di database.
Langkah ini diperlukan agar sistem dapat kembali memverifikasi batasan foreign key setelah tabel "customers" dikosongkan.

===========================================
